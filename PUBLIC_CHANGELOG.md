# What's New

Player-facing release notes from **v0.4.0** onward live as per-version posts under `content/changelog/public/`.

Pre-0.4.0 history is frozen in `content/changelog/legacy/PUBLIC_CHANGELOG.md` and rendered at `/changelog/legacy`.

When updating release notes, edit or add `content/changelog/public/{version}.md`.
