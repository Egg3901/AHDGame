/**
 * SCO uses the United Kingdom's bucket labels -- the same terms, for the same
 * electorate, before and after secession.
 *
 * ⚠️ A RE-EXPORT, NOT A COPY. The table used to alias the UK object directly;
 * re-exporting keeps that one object, so the two stay identical by reference
 * rather than by anyone remembering to edit both.
 */
export { UK_BUCKET_LABELS as SCO_BUCKET_LABELS } from "@/lib/countries/uk/data/ukBucketLabels";
