/**
 * Moved into the rules zone (`src/lib/banking/rules/balanceSheet.ts`). This path is kept
 * so existing imports resolve to the one implementation.
 */
export * from "@/lib/banking/rules/balanceSheet";
