/**
 * Moved into the rules zone (`src/lib/banking/rules/creditBands.ts`). This path is kept
 * so existing imports resolve to the one implementation.
 */
export * from "@/lib/banking/rules/creditBands";
