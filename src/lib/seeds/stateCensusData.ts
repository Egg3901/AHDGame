/**
 * Forwarder. Moved to the United States' country folder.
 *
 * ⚠️ A FORWARDER HOLDS NO COPY. `stateCensusData` has exactly one definition, at
 * `@/lib/countries/us/data/usStateCensusData`.
 *
 * ⚠️ IT IS US DATA AND SAID SO NOWHERE. Keyed purely by state code, in a
 * shared seeds directory, under a name that names no country. Only the keys
 * gave it away -- the United States is the one country whose region ids are
 * bare two-letter codes.
 */
export * from "@/lib/countries/us/data/usStateCensusData";
