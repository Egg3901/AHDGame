/**
 * Shared constants for the parliamentary government formation system.
 * Used by all parliamentary countries (UK, JP, CA, DE).
 */

/** Hours until a PM appointment or no-confidence vote closes. */
export const PM_VOTE_DURATION_HOURS = 24;

/** Cooldown (in turns) after a no-confidence vote before another can be proposed. */
export const NO_CONFIDENCE_COOLDOWN_TURNS = 48;
