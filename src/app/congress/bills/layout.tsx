/**
 * Bills layout — ensures bills/[id] segment has proper nesting for error boundaries.
 */
export default function BillsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
