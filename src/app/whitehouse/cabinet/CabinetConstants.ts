/**
 * Cabinet page hero image.
 * Wikimedia Commons — White House Cabinet Room (public domain).
 * Fallback gradient used if image fails (see HeroImage).
 */
export const CABINET_HERO = {
  image: "/api/images/hero/cabinet",
  title: "Cabinet",
  tagline: "15 principal officers · Advice and consent of the Senate",
  alt: "White House Cabinet Room",
} as const;
