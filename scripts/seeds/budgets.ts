/**
 * Re-export — canonical DB seed module: `src/lib/seeds/reference/budgets.ts`.
 * Scripts and legacy imports keep using `./seeds/budgets`.
 */
export * from "../../src/lib/seeds/reference/budgets";
