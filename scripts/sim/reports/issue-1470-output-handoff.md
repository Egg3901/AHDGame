# Issue 1470 output measurement handoff

Generated by `scripts/sim/outputMeasurementHandoff.ts` on 2026-09-06. This is a deterministic kernel harness using synthetic public-safe signals. It does not read or write a database.

The harness exercises the production `blendOutputGrowthSignal`, constant-price `outputVolume`, revenue EMA/snapshot helpers, and `advanceOutputGap`. It verifies the identity `gdpGrowth = potential + (newGap - previousGap) * turnsPerYear` on every simulated turn.

Each case has 56 turns of mature nominal history. Both the physical-output EMA and its history start at the cutover. Quantity shocks occur 24 turns later, after a physical baseline exists; price shocks occur at cutover. The output history span is 8 turns at the first usable observation and reaches 48 turns at the full handoff. The harness covers a one-time nominal price rise, a one-time nominal price fall, a real quantity rise, and a real quantity fall.

| Case                                            | Turn 8 blended signal | Turn 9 blended signal | Turn 48 blended signal | Turn 49 blended signal | Turn 64 blended signal |
| ----------------------------------------------- | --------------------: | --------------------: | ---------------------: | ---------------------: | ---------------------: |
| rising nominal price, constant physical output  |                 7.684 |                 7.671 |                  0.000 |                  0.000 |                  0.000 |
| falling nominal price, constant physical output |                -7.684 |                -7.671 |                  0.000 |                  0.000 |                  0.000 |
| constant price, rising physical output          |                 0.000 |                 0.000 |                  9.828 |                  9.653 |                  9.987 |
| constant price, falling physical output         |                 0.000 |                 0.000 |                 -9.828 |                 -9.653 |                 -9.987 |

At turn 8 the blend weight is zero, so the newly available physical signal cannot create a handoff cliff. At turn 48 the weight is one, and after turn 48 the blended value equals the physical signal with no nominal contribution. The constant-physical price cases therefore end at exactly zero despite their large nominal signals. The quantity cases retain the real physical change, including the expected clamped early falling signal and its later EMA recovery.

The output-gap identity assertion passed for every row and every unreported simulated turn. No gap reset or post-integration GDP blending was used. The old gap is allowed to close through the same production integrator, preserving coherence between the growth rate and stored gap.

The earlier transition diagnosis needs one correction: the candidate-versus-control GDP differences are mostly sector-signal differences. A pre-existing nominally accumulated output gap explains the negative candidate growth level during unwinding, but the paired results do not prove that short-history noise is absent. The handoff harness isolates that risk: short history is held at the prior signal, the transition is linear from span 8 to 48, and mature physical history removes price-only movement permanently.

Recommendation: retain the implemented global signal blend. It provides a mild transition without national special cases, prevents a new measurement from changing GDP at the first 8-turn observation, reaches full physical authority at 48 turns, and preserves the output-gap state and identity throughout.
